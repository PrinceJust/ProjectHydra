import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import ActionButton from './index';

export const argTypesButton = {
  variant: {
    description: 'Variants for the button. Available button colors `default`, `info`, `warning`, `danger` ',
    defaultValue: {
      summary: 'default'
    }
  },
  text: {
    description: 'Button text display.'
  },
  sizes: {
    description: 'Size for the buttons. Available sizes `sm`, `md`, `lg`',
    defaultValue: {
      summary: 'md'
    }
  },
  disabled: {
    description: 'The status of the button whether it is disabled or not.',
    defaultValue: {
      summary: 'false'
    }
  },
  click: {
    description: 'The event occur when the user `click` the action button.'
  }
}

<Meta title="MDX/ActionButtons/asd" component={ ActionButton }
  argTypes={ argTypesButton }
/>

export const Template = (args) => <ActionButton { ...args } />

# Buttons
Custom button styles for actions in forms, dialogs, and more with support for multiple sizes, states, and more.

# Variants
Use any of the available button style types to quickly create a styled button. Just modify the **variant** prop.
<Canvas>
  <Story name="Varaint Default" args={
    {
      text : 'Default',
      sizes : 'md',
      variant : 'default',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
  <Story name="Varaint Info" args={
    {
      text : 'Info',
      sizes : 'md',
      variant : 'info',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
  <Story name="Varaint Warning" args={
    {
      text : 'Warning',
      sizes : 'md',
      variant : 'warning',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
  <Story name="Varaint Danger" args={
    {
      text : 'Danger',
      sizes : 'md',
      variant : 'danger',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
</Canvas>

# Sizes
Fancy larger or smaller buttons? Add **sizes="lg"**, **sizes="sm"** for additional sizes.

<Canvas>
  <Story name="Size Small" args={
    {
      text : 'Small Button',
      sizes : 'sm',
      variant : 'info',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
  <Story name="Size Medium" args={
    {
      text : 'Medium Button',
      sizes : 'md',
      variant : 'warning',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
  <Story name="Size Large" args={
    {
      text : 'Large Button',
      sizes : 'lg',
      variant : 'danger',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
</Canvas>

# Disabled
Make buttons look inactive by adding the **disabled** prop to.

<Canvas>
  <Story name="Disabled" args={
    {
      text : 'Text Button',
      sizes : 'md',
      variant : 'default',
      disabled : true,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
</Canvas>

# Text
Customable button text add **text='Text Button'** prop.

<Canvas>
  <Story name="Text" args={
    {
      text : 'Text Button',
      sizes : 'md',
      variant : 'info',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
</Canvas>


# Default button
You can modify this button using **controls**.

<Canvas>
  <Story name="Default" args={
    {
      text : 'Default',
      sizes : 'md',
      variant : 'default',
      disabled : false,
      click: () => {}
    }
  }>
    { Template.bind({ }) }
  </Story>
</Canvas>

<ArgsTable story="Default" />

