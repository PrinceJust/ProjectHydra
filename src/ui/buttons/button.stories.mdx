import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import ActionButton from './index'

export const argTypesButton = {
  text: {
    description: 'Button text display.'
  },
  btnSize: {
    description: 'Size for the buttons. Available sizes `sm`, `md`, `lg`',
    defaultValue: {
      summary: 'md'
    }
  },
  disabled: {
    description: 'The status of the button whether it is disabled or not.',
    defaultValue: {
      summary: 'false'
    }
  },
  click: {
    description: 'The event occur when the user `click` the action button.'
  },
  variant: {
    description: 'Variants for the button. Available button colors `default`, `info`, `warning`, `danger` ',
    defaultValue: {
      summary: 'default'
    }
  },
}

<Meta title="MDX/Action Buttons" component={ ActionButton }
  argTypes={ argTypesButton }
/>

export const Template = (args) => <ActionButton { ...args } />

# Action Buttons
Custom button styles for actions in forms, dialogs, and more with support for multiple sizes, states, and more.

# Variants
Use any of the available button style types to quickly create a styled button. Just modify the **variant** prop.
<Canvas>
  <Story name="Varaint Default" args={
    {
      btnSize : 'md',
      click: () => {},
      text : 'Default',
      disabled : false,
      variant : 'default',
    }
  }>
    { Template.bind({}) }
  </Story>
  <Story name="Varaint Info" args={
    {
      text : 'Info',
      btnSize : 'md',
      click: () => {},
      variant : 'info',
      disabled : false,
    }
  }>
    { Template.bind({}) }
  </Story>
  <Story name="Varaint Warning" args={
    {
      btnSize : 'md',
      click: () => {},
      text : 'Warning',
      disabled : false,
      variant : 'warning',
    }
  }>
    { Template.bind({}) }
  </Story>
  <Story name="Varaint Danger" args={
    {
      btnSize : 'md',
      click: () => {},
      text : 'Danger',
      disabled : false,
      variant : 'danger',
    }
  }>
    { Template.bind({}) }
  </Story>
</Canvas>

# Sizes
Fancy larger or smaller buttons? Add **btnSize="lg"**, **btnSize="sm"** for additional sizes.

<Canvas>
  <Story name="Size Small" args={
    {
      btnSize : 'sm',
      click: () => {},
      variant : 'info',
      disabled : false,
      text : 'Small Button',
    }
  }>
    { Template.bind({}) }
  </Story>
  <Story name="Size Medium" args={
    {
      btnSize : 'md',
      click: () => {},
      disabled : false,
      variant : 'warning',
      text : 'Medium Button',
    }
  }>
    { Template.bind({}) }
  </Story>
  <Story name="Size Large" args={
    {
      btnSize : 'lg',
      click: () => {},
      disabled : false,
      variant : 'danger',
      text : 'Large Button',
    }
  }>
    { Template.bind({}) }
  </Story>
</Canvas>

# Disabled
Make buttons look inactive by adding the **disabled** prop to.

<Canvas>
  <Story name="Disabled" args={
    {
      btnSize : 'md',
      click: () => {},
      disabled : true,
      variant : 'default',
      text : 'Text Button',
    }
  }>
    { Template.bind({}) }
  </Story>
</Canvas>

# Text
Customable button text add **text='Text Button'** prop.

<Canvas>
  <Story name="Text" args={
    {
      btnSize : 'md',
      click: () => {},
      variant : 'info',
      disabled : false,
      text : 'Text Button',
    }
  }>
    { Template.bind({}) }
  </Story>
</Canvas>


# Default button
You can modify this button using **controls**.

<Canvas>
  <Story name="Default" args={
    {
      btnSize : 'md',
      click: () => {},
      text : 'Default',
      disabled : false,
      variant : 'default',
    }
  }>
    { Template.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="Default" />

